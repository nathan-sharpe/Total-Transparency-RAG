[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# Defaults (E pycodestyle, F pyflakes) plus import sorting (I),
# common bug patterns (B), and modern-syntax upgrades (UP).
select = ["E", "F", "I", "B", "UP"]
