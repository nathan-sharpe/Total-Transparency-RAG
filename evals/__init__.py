"""Evaluation harness: golden set, retrieval metrics, and eval runners.

Eval scripts import the rag/ pipeline functions directly (never over HTTP) so
the numbers measure the same code the API serves.
"""
